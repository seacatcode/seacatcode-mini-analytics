; var seaCatCodeMiniAnalytics = (function (O) {

    O.host = O.host === void 0 ? '/' : O.host;

    function send() {
        var xhr = new XMLHttpRequest();
        xhr.open('POST', '/'); // 메소드와 주소 설정
    }

    return O;
})(seaCatCodeMiniAnalytics === void 0 ? {} : seaCatCodeMiniAnalytics);